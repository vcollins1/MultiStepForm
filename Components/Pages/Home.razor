@page "/"

<PageTitle>MultiStep Form</PageTitle>

<main class="multi-step">
    <StepIndicatorDisplay Steps="@_steps" CurrentStep="@(_currentStepIndex + 1)" />
    <section class="main-section">
        <StepDisplay CurrentStep="_currentStepIndex" OnValidSubmit="@NextDisplay" />
        <StepDisplayControls
            OnNext="@NextDisplay"
            OnBack="@PreviousDisplay"
            CurrentStep="_currentStepIndex"
        />
    </section>
</main>

@code 
{
    private readonly string[] _steps = ["YOUR INFO", "SELECT PLAN", "ADD-ONS", "SUMMARY"];

    private int _currentStepIndex = 1;

    private void NextDisplay()
    {
        _currentStepIndex = Math.Min(_steps.Length - 1, _currentStepIndex + 1);
    }

    private void PreviousDisplay()
    {
        _currentStepIndex = Math.Max(0, _currentStepIndex - 1);
    }
}
